#!/bin/bash
# Launches steam. Steam does not launch itself properly with the 'steam' command
# under arch linux, but this script does. Obviously, requires steam to be
# installed.
LD_PRELOAD='/usr/$LIB/libstdc++.so.6 /usr/$LIB/libgcc_s.so.1 /usr/$LIB/libxcb.so.1 /usr/$LIB/libgpg-error.so' steam
